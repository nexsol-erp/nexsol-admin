node_modules
build/
public/downloads/*.exe

# pos-electron build artifacts (source files are committed, built outputs are not)
pos-electron/node_modules
pos-electron/dist
pos-electron/release
pos-electron/.electron-cache

# Database dumps. These are production tenant data - never commit them, and note that
# 9446968394a.dump is ~645MB, which git would carry forever even after a later delete.
*.dump
*.dump.gz
